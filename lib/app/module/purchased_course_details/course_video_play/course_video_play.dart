import 'package:flutter/material.dart';

class CourseVideoPlay extends StatelessWidget {
  const CourseVideoPlay({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("paid course video"),
      ),
    );
  }
}
